# Progress Log

## Current Verified State

- Last Updated: 2026-07-05
- Repository root: D:\AMD-HACKATHON-II
- Current Objective: Build the concept-dependency-graph pipeline end to end (feat-001 → feat-009 in `feature_list.json`). feat-001 through feat-008 are passing; feat-009 (pre-indexed, deterministic demo dataset) is **blocked** on real Gemma/Fireworks infrastructure specifically, but the pipeline's actual logic has now been validated live end-to-end against GPT-4o mini (see Session 012) — a prototyping-only path, not a substitute for the real submission run. Separately, `apps/web` was restyled to the "Atlas Learning System" design system (Session 011) — a visual pass over already-passing features, not a new feature ID.
- Standard startup path: `./init.sh`
- Standard verification path: see "Verification Commands" in `CLAUDE.md` — web lint/build, api/gpu-worker `compileall`, `cd apps/api && python -m pytest tests/` (25/25 passing), and `cd packages/gpu-worker && python -m pytest tests/` (19/19 passing). Additionally (not yet folded into `CLAUDE.md`'s fixed list since it's a one-off tool, not one of the three services): `python -m pytest scripts/tests/` (3/3 passing).
- Highest-priority unfinished feature: feat-009, "Pre-indexed, deterministic demo dataset" — status `blocked`, see feature_list.json's notes for the exact unblock steps.
- Blockers: **feat-009 is blocked** on the real Gemma+Fireworks run specifically (required for the AMD-usage/Best-of-Gemma judging criteria) — this environment still has neither a reachable `GEMMA_BASE_URL` nor a `FIREWORKS_API_KEY`. An `LLM_PROVIDER=openai` prototyping path (Session 012) now lets the same pipeline run against GPT-4o mini instead, which was used this session to prove the pipeline's composition logic works end-to-end — but that does NOT unblock this feature; it's a separate, complementary de-risking step. There is still no persisted chunk-text or completed-status storage layer anywhere in the repo (flagged in feat-007/feat-008's notes) — orthogonal to feat-009 for now since the demo dataset bakes chunk text directly into the generated JSON's per-node sources.
- Recommended Next Step: once a machine has `GEMMA_BASE_URL` and `FIREWORKS_API_KEY` available, run `python scripts/build_demo_dataset.py` (reads `data/source_docs/`, a real checked-in 5-document intro-ML set, writes to `data/generated/demo_tree.generated.json` by default). Before that real run, though, consider Session 012's two calibration findings (concept-dedup threshold and prerequisite pre-filter threshold both need re-tuning per embedding backend — don't assume the current 0.35/0.9 defaults are right for whatever embedding model the real run uses without checking). Then decide whether/how to promote the output to replace `data/atlas_mastery_tree_sample.json` — doing so requires updating `apps/api/tests/test_trees_endpoint.py` and `test_trees_submit_quiz_endpoint.py`'s hardcoded node/question IDs (`n3`, `q_n3_1`, etc.) in the same pass, since they assert against the current hand-written file's specific IDs.

## Session Log

### Session 001

- Date: 2026-07-05
- Goal: Consolidate and de-duplicate `docs/` (5 overlapping/contradictory documents → 4 consistent ones), then scaffold a minimal harness.
- Completed:
  - Merged `Approach...md` + `Concept_Graph_Engine_Detail.md` → `docs/concept-graph-pipeline.md`.
  - Split the concatenated `Atlas_Hackathon_Feature_Prioritization.md` into a pitch section (moved to `docs/brd.md`) and a scope doc (`docs/hackathon-scope.md`), renumbered, merged its two redundant 7-day plans into one.
  - Rewrote `docs/BRD_Ver_1.0_Atlas.md` → `docs/brd.md`: replaced the gamification/flashcards/Postgres MVP scope with the actual graph/skill-tree MVP scope (added §7.1b Post-MVP Roadmap), fixed tech stack to match the real repo (Next.js, SQLite/JSON, Gemma served locally on ROCm), resolved 4 of 6 open clarifying questions.
  - Updated `docs/architecture.md` to link out instead of duplicating pipeline detail.
  - Created harness scaffold via `harness-creator` skill: `CLAUDE.md`, `feature_list.json` (feat-001..009, mapped from `docs/hackathon-scope.md` §4 P0 table), `progress.md`, `session-handoff.md`, `init.sh`.
  - Fixed a bug in the generated `init.sh` (each `cd` line wasn't subshell-isolated, so the second `cd apps/web` would have failed on the third command).
- Verification run: manually ran the two Python commands (`cd apps/api && python -m compileall app`, `cd packages/gpu-worker && python -m compileall worker`) — both pass. Did not run the web `lint`/`build` commands or the full `./init.sh` — `apps/web/node_modules` isn't installed yet (`npm install` needed first). Next session should install web deps and run `./init.sh` once before starting feat-001.
- Evidence captured: none (no feature work done this session — this was a docs/harness session).
- Commits: none made this session (docs and harness files are on disk, uncommitted).
- Files or artifacts updated: see "Completed" above, plus this harness scaffold.
- Known risk or unresolved issue: two `brd.md` clarifying questions remain genuinely open (primary target audience; public-track sharing/licensing) — see `docs/brd.md` §2.1.
- Next best step: run `./init.sh` to confirm the baseline is green, then start feat-001 (ingest & chunking) in `packages/gpu-worker`.

### Session 002

- Date: 2026-07-05
- Goal: Implement feat-001, "Document ingest & chunking."
- Completed: Added `packages/gpu-worker/worker/ingest.py` (`chunk_document()` dispatching to PDF/PPTX/DOCX parsers, chunking by page/slide/heading boundary rather than a hard character count); wired a new `POST /ingest` endpoint in `worker/main.py` (kept `/embed` reserved for feat-002); added parsing deps (`pymupdf`, `python-pptx`, `python-docx`, `python-multipart`) to `requirements.txt` and a new `requirements-dev.txt` for `pytest`; added `packages/gpu-worker/tests/test_ingest.py` with synthetic PDF/PPTX/DOCX fixtures.
- Verification run: `python -m compileall worker tests` (pass); `python -m pytest tests/` — 4/4 passed; manually exercised `POST /ingest` via FastAPI `TestClient` with a synthetic PDF (200, correct chunk shape).
- Evidence captured: recorded in `feature_list.json` feat-001 `evidence[]`.
- Commits: not yet committed this session.
- Files or artifacts updated: `packages/gpu-worker/worker/ingest.py` (new), `packages/gpu-worker/worker/main.py`, `packages/gpu-worker/requirements.txt`, `packages/gpu-worker/requirements-dev.txt` (new), `packages/gpu-worker/tests/test_ingest.py` (new), `init.sh`, `CLAUDE.md`, `feature_list.json`.
- Known risk or unresolved issue: no real sample PDF/PPTX/DOCX exists in `data/` yet — tests use synthetic in-memory fixtures. Re-verify against the actual demo dataset once feat-009 picks one. docx chunks use a heading-delimited section index as `page` (no true page number available without rendering) — documented in `ingest.py` and worth flagging in the demo if judges ask.
- Next best step: implement feat-002 (embedding + local Gemma concept extraction + clustering) on top of `chunk_document()`.

### Session 003

- Date: 2026-07-05
- Goal: Implement feat-002, "Embedding + concept extraction & clustering."
- Completed: Added `packages/gpu-worker/worker/concepts.py` — `GemmaConceptExtractor` (HTTP client against a local Ollama-style endpoint, `GEMMA_BASE_URL` env var, default `http://localhost:11434`, no hosted-API path), `SentenceTransformerEmbedder` (lazy-loaded `bge-large-en-v1.5`), and `cluster_concepts` (cosine similarity + `networkx.algorithms.community.louvain_communities`, no LLM call, per docs/concept-graph-pipeline.md step 3). Wired `POST /embed` in `worker/main.py` via FastAPI `Depends` (`get_gemma_extractor`, `get_embedder`) so both can be swapped for fakes in tests. Added `tests/test_concepts.py` (4 tests) and `tests/test_main_embed.py` (endpoint-level test using `app.dependency_overrides`). Added `numpy`/`httpx` to `requirements.txt` (previously only transitive deps).
- Verification run: `python -m compileall worker tests` (pass); `python -m pytest tests/` — 9/9 passed (4 feat-001 + 5 feat-002, no regressions).
- Evidence captured: recorded in `feature_list.json` feat-002 `evidence[]`, including the specific assertion that `GemmaConceptExtractor` posts to `{base_url}/api/generate` (a regression to a hosted API URL would fail that test) and the near-duplicate-merge clustering test ("gradient descent" vs "the steepest-descent optimization algorithm").
- Commits: not yet committed this session.
- Files or artifacts updated: `packages/gpu-worker/worker/concepts.py` (new), `packages/gpu-worker/worker/main.py`, `packages/gpu-worker/requirements.txt`, `packages/gpu-worker/tests/test_concepts.py` (new), `packages/gpu-worker/tests/test_main_embed.py` (new), `feature_list.json`.
- Known risk or unresolved issue: not verified against a real local Gemma/Ollama server or a real sentence-transformers model download — both are behind fakes in the test suite by design. Concept embeddings aren't cached/returned from `/embed`; feat-003 will need embeddings again for pair pre-filtering, so recomputation cost is worth watching.
- Next best step: implement feat-003 (Fireworks-based prerequisite inference with O(n²)-avoidance pre-filtering) in `apps/api`.

### Session 004

- Date: 2026-07-05
- Goal: Implement feat-003, "Dependency-graph construction (prerequisite inference)."
- Completed: Discovered and fixed an inconsistency between `feature_list.json` (which said feat-003 lives in `apps/api`) and `docs/architecture.md`'s Data contract section (which says `gpu-worker` produces the candidate graph including inferred edges, and `api` starts at the self-checking loop) — corrected `feature_list.json`'s `area` to `gpu-worker` and implemented there instead. Gave `CanonicalConcept` (feat-002) an `id` (`concept_NNN`) and `embedding` (cluster centroid) field so `/embed`'s output carries what pre-filtering needs, without apps/api needing its own embedding model. Added `packages/gpu-worker/worker/prerequisites.py`: `pre_filter_pairs` (cosine similarity over concept embeddings) and `FireworksClient` (hosted OpenAI-compatible chat completions — a hosted call is fine here, unlike Gemma) and `build_candidate_edges`. Wired `POST /build-graph` via FastAPI `Depends`. Added `tests/test_prerequisites.py` (4 tests, including one that directly proves the O(n²) avoidance: 6 concepts, C(6,2)=15 possible pairs, but only 3 pre-filtered pairs are ever sent to the fake Fireworks client) and `tests/test_main_build_graph.py` (endpoint-level).
- Verification run: `python -m compileall worker tests` (pass); `python -m pytest tests/` — 14/14 passed (4 feat-001 + 5 feat-002 + 5 feat-003, no regressions after the `CanonicalConcept` schema change).
- Evidence captured: recorded in `feature_list.json` feat-003 `evidence[]`, including the exact call-count proof for the O(n²)-avoidance verification bullet. Also corrected feat-002's stale note (previously said embeddings weren't returned from `/embed` — now they are).
- Commits: not yet committed this session.
- Files or artifacts updated: `packages/gpu-worker/worker/concepts.py` (CanonicalConcept: +id, +embedding), `packages/gpu-worker/worker/prerequisites.py` (new), `packages/gpu-worker/worker/main.py`, `packages/gpu-worker/tests/test_prerequisites.py` (new), `packages/gpu-worker/tests/test_main_build_graph.py` (new), `feature_list.json`, `progress.md`.
- Known risk or unresolved issue: not verified against a real Fireworks API key/live endpoint — `FireworksClient` is only verified against a monkeypatched `httpx.post`. `candidate_edges[]` haven't been spot-checked against the actual sample tree in `docs/concept-graph-pipeline.md` yet (needs a real Fireworks key + a real feat-002 run); do that once feat-009 picks the demo dataset. Edge `from`/`to` use concept `id` (e.g. `concept_001`), not name — worth confirming that's what feat-004/feat-005/feat-006 expect downstream.
- Next best step: implement feat-004 (self-checking graph agent — cycle detection + repair loop) in `apps/api`, consuming feat-003's `candidate_edges[]`.

### Session 005

- Date: 2026-07-05
- Goal: Implement feat-004, "Self-checking graph agent (cycle/redundancy repair)."
- Completed: Added `apps/api/app/services/graph.py` — `build_graph` (candidate_edges[] → `networkx.DiGraph` with a `confidence` edge attribute), `repair_cycles` (bounded loop: on each iteration, find all cycles via `nx.simple_cycles()`, drop the lowest-confidence edge in the shortest one; after `max_iterations`, fall back to an unbounded while-loop that force-drops the weakest edge in any remaining cycle so the agent always terminates with a valid DAG), and `validate_graph` (orchestrates both, returns `{"edges": ..., "dropped_edges": [...]}` — the drop log with a `reason` per entry is what makes this auditable as an "agentic self-check" rather than a silent filter). Wired a new `POST /graph/validate` endpoint. This is `apps/api`'s first feature with real logic, so also set up its first test suite (`apps/api/tests/`, `apps/api/requirements-dev.txt`) and wired `pytest` into `init.sh`/`CLAUDE.md`.
- Verification run: `python -m compileall app tests` (pass); `python -m pytest tests/` in `apps/api` — 6/6 passed, including a test that specifically forces the force-drop fallback path (two independent 2-cycles, `max_iterations=1`, only 1 of the 2 needed drops fits the bounded budget) and confirms the result is still acyclic. Also re-ran `packages/gpu-worker`'s suite — 14/14, no regressions.
- Evidence captured: recorded in `feature_list.json` feat-004 `evidence[]`.
- Commits: not yet committed this session.
- Files or artifacts updated: `apps/api/app/services/__init__.py` (new), `apps/api/app/services/graph.py` (new), `apps/api/app/routers/graph.py` (new), `apps/api/app/main.py`, `apps/api/requirements-dev.txt` (new), `apps/api/tests/__init__.py` (new), `apps/api/tests/test_graph.py` (new), `apps/api/tests/test_graph_endpoint.py` (new), `init.sh`, `CLAUDE.md`, `feature_list.json`.
- Known risk or unresolved issue: `nx.simple_cycles()` is exponential in the worst case — fine for the hackathon's sparse, pre-filtered graphs, but worth watching if the real demo dataset ends up much larger/denser than expected. Not yet run on a real candidate-edge set from a live feat-003 run (Fireworks) — only synthetic edge lists in tests.
- Next best step: implement feat-005 (topological sort via `nx.topological_generations()`, tier assignment) in `apps/api`, and wire `GET /trees/{topic_id}` to return real data instead of its 501 stub.

### Session 006

- Date: 2026-07-05
- Goal: Implement feat-005, "Topological sort → tiered learning path."
- Completed: Added `apps/api/app/services/levels.py` — `assign_levels(edges, concepts)` builds a `DiGraph` seeded with every concept `id` (not just ones with an edge, so isolated concepts still get a node), groups via `nx.topological_generations()`, and returns `nodes[]` with `id`/`name`/`level`/`sources`/`status` (tier 0 = unlocked, everything else = locked). Wired `GET /trees/{topic_id}` in `apps/api/app/routers/trees.py` to serve `data/atlas_mastery_tree_sample.json` (path resolved from repo root, overridable via `SAMPLE_TREE_PATH`) instead of its `HTTPException(501)` — this was literally the file's own pre-existing TODO comment's recommendation, not a new call.
- Verification run: `python -m compileall app tests` (pass); `python -m pytest tests/` in `apps/api` — 11/11 passed (5 feat-004 + 6 feat-005, no regressions). Re-ran `packages/gpu-worker`'s suite — 14/14, no regressions.
- Evidence captured: recorded in `feature_list.json` feat-005 `evidence[]`, including a test that mirrors docs/concept-graph-pipeline.md's own sample tree (two branches converging) and directly asserts the DAG tier invariant, plus an endpoint test that re-checks that invariant against the real checked-in sample dataset (not just synthetic test data).
- Commits: not yet committed this session.
- Files or artifacts updated: `apps/api/app/services/levels.py` (new), `apps/api/app/routers/trees.py`, `apps/api/tests/test_levels.py` (new), `apps/api/tests/test_trees_endpoint.py` (new), `feature_list.json`, `progress.md`.
- Known risk or unresolved issue: `GET /trees/{topic_id}` is explicitly NOT wired to the live pipeline yet — it serves the static sample file regardless of `topic_id`; composing a real feat-002→feat-005 run into this endpoint is a bigger integration task, likely alongside feat-009. `data/` isn't copied into the Docker image (`docker/api.Dockerfile` only `COPY`s `apps/api/`) — `SAMPLE_TREE_PATH` is the escape hatch, not yet used. Also noticed while reading the sample dataset: it uses `"completed"` for a mastered node's status, while `docs/concept-graph-pipeline.md`'s schema says `"mastered"` — a naming mismatch to reconcile when feat-006/008 build the real unlock mechanic.
- Next best step: implement feat-006 (skill-tree UI with unlock logic) in `apps/web`, replacing the `TODO: render mastery tree` placeholder in `app/tree/page.tsx`. Requires `npm install` in `apps/web` first (node_modules still not installed as of this session).

### Session 007

- Date: 2026-07-05
- Goal: `npm install` in `apps/web`, then implement feat-006, "Skill-tree UI with unlock logic."
- Completed: Ran `npm install` in `apps/web` (411 packages; flagged a known `next@14.2.5` security advisory — not addressed, out of scope, noted for awareness rather than silently upgraded). Confirmed baseline lint/build passed on the pre-existing placeholder pages before touching anything. Implemented the tree view across `apps/web/app/tree/`: `types.ts` (shared shapes), `unlock.ts` (derives locked/unlocked/completed status from a single source of truth — a completed-id set — plus edge structure, rather than trusting a possibly-stale `status` field), `positions.ts` (layout into columns by level), `progressStore.ts` (zustand store holding `completedIds` client-side so unlocks react without a page reload), `TreeNode.tsx` (custom React Flow node, colored via the `locked`/`unlocked`/`completed` colors already defined in `tailwind.config.ts`), and `page.tsx` (fetches `GET {NEXT_PUBLIC_API_URL}/trees/{topic_id}`, renders via `@xyflow/react`).
- Verification run: `npm run lint` (clean) and `npm run build` (compiled + type-checked) — the build caught a real bug: a file named `layout.ts` collides with Next.js App Router's reserved filename inside `app/`, renamed to `positions.ts` to fix. Beyond typecheck/build, actually ran the app: started `apps/api` (uvicorn) and `apps/web` (`next dev`) in the background, installed Playwright temporarily, and drove a real headless-Chromium session against `/tree`. Confirmed visually (screenshots inspected): tier-0 completed nodes render green, the unlocked node renders blue, locked nodes render grey, grouped into columns by level; clicking the unlocked "Loss Functions" node turned it green and immediately flipped its child "Gradient Descent" from grey/locked to blue/unlocked with no page reload and no browser console errors, while the further "Neural Networks" node correctly stayed locked. Re-ran `apps/api` (11/11) and `packages/gpu-worker` (14/14) test suites afterward — no regressions from this frontend-only change. Cleaned up: killed both background dev servers, removed the temporary Playwright install.
- Evidence captured: recorded in `feature_list.json` feat-006 `evidence[]`, including the screenshot-based manual verification (not just typecheck) and the layout.ts naming-collision bug found and fixed along the way.
- Commits: not yet committed this session.
- Files or artifacts updated: `apps/web/app/tree/{types.ts, unlock.ts, positions.ts, progressStore.ts, TreeNode.tsx, page.tsx}` (new/replaced), `feature_list.json`, `progress.md`. Also fixed the `mastered`→`completed` terminology mismatch flagged in the previous session's handoff: updated `docs/concept-graph-pipeline.md` and `docs/brd.md` (3 occurrences total) to say `completed`, matching `tailwind.config.ts`'s actual color names and the sample dataset — the doc wording was the stale one.
- Known risk or unresolved issue: the click-to-complete interaction is a demo-only stand-in for feat-008's real checkpoint-quiz submission (any click on an unlocked node "passes" it instantly, no quiz UI yet). `next@14.2.5` has a known security advisory per `npm install`'s own warning — not upgraded, flagging for awareness since a version bump could be a breaking change worth doing deliberately, not incidentally.
- Next best step: implement feat-007 (per-node lesson/quiz/example via agentic RAG) in `apps/api` — this is what feat-008's real quiz UI will eventually call into.

### Session 008

- Date: 2026-07-05
- Goal: Implement feat-007, "Per-node lesson + quiz + real-world example (agentic RAG)."
- Completed: Added `apps/api/app/services/teach.py` — `FireworksClient` (hosted chat-completions client, separate instance from gpu-worker's step-4 `FireworksClient` since the two services deploy independently) with `generate()` (lesson + MCQ quiz + real-world example, prompted only with the given chunks' excerpts) and `check_alignment()` (the self-check: does the quiz actually test the lesson's content, and is the marked answer correct), and `generate_lesson_package()` — a bounded retry loop (default 2 attempts) that regenerates the whole package on a failed alignment check and, if every attempt still fails, returns the last attempt flagged with `self_check.passed = False` instead of raising (so a stubborn misalignment degrades gracefully in a demo rather than 500ing). Wired `POST /trees/{topic_id}/nodes/{node_id}/lesson` in `apps/api/app/routers/teach.py` via `Depends(get_fireworks_client)`, same override pattern as gpu-worker's `/embed`/`/build-graph`.
- Verification run: `python -m compileall app tests` (pass); `python -m pytest tests/` in `apps/api` — 16/16 passed (11 pre-existing + 5 new, no regressions), including a forced-mismatch test proving the self-check guard actually triggers a second `generate()` call (not just a re-check) and a forced-always-fail test proving the function still returns a result rather than raising. Re-ran `packages/gpu-worker` — 14/14, no regressions. Full `./init.sh` (web lint/build + both python suites) — all green.
- Evidence captured: recorded in `feature_list.json` feat-007 `evidence[]`.
- Commits: not yet committed this session.
- Files or artifacts updated: `apps/api/app/services/teach.py` (new), `apps/api/app/routers/teach.py` (new), `apps/api/app/main.py`, `apps/api/tests/test_teach.py` (new), `apps/api/tests/test_teach_endpoint.py` (new), `feature_list.json`, `progress.md`.
- Known risk or unresolved issue: apps/api has no persisted chunk-text store — chunks only ever existed ephemerally inside a single gpu-worker `/ingest`→`/embed` request, and `docs/hackathon-scope.md` §5's SQLite/JSON storage line item hasn't been built as its own feature. Rather than inventing a store as a side effect of this feature, the new endpoint takes the node's chunks[] directly in the request body — scoping to "this node's own sources only" is enforced by the request shape, not by a filter over a larger store. This needs to be reconciled with feat-009 (pre-indexed demo dataset), which will need to decide where chunk text is persisted long-term so the real `/trees/{topic_id}` flow can supply it. Not verified against a live Fireworks API — `FireworksClient` is only verified against a fake in tests.
- Next best step: implement feat-008 (checkpoint quiz & unlock mechanic) in `apps/web`, wiring the quiz UI to `POST /trees/{topic_id}/nodes/{node_id}/submit-quiz` (currently a 501 stub) and reusing feat-006's existing `progressStore`/unlock logic on a pass.

### Session 009

- Date: 2026-07-05
- Goal: Implement feat-008, "Checkpoint quiz & unlock mechanic."
- Completed: Backend — added `apps/api/app/services/quiz.py` (`grade_quiz(quiz, answers)`, a pure scoring function: missing answers count as wrong, score = correct/total, passed = score >= pass_threshold) and replaced `apps/api/app/routers/trees.py`'s `submit_quiz` 501 stub with a real implementation (refactored tree-loading into a shared `_load_tree()` helper used by both `GET /trees/{topic_id}` and the new endpoint; 404s for an unknown node_id). Frontend — added `apps/web/app/tree/QuizModal.tsx` (MCQ radio-button quiz UI, disabled Submit until all questions answered, inline pass/fail feedback), extended `types.ts`/`unlock.ts` to carry each node's `quiz` field through to the display layer, and replaced `page.tsx`'s feat-006 demo stand-in ("any click on an unlocked node instantly completes it") with a real flow: click opens the quiz modal, submitting POSTs to `/submit-quiz`, and `markCompleted()` (feat-006's existing store) is only called when the response's `passed` is true.
- Verification run: `python -m compileall app tests` + `python -m pytest tests/` in `apps/api` — 23/23 passed (16 pre-existing + 7 new: 4 grading-logic tests including an exact-boundary case, 3 endpoint tests against the real sample dataset for pass/fail/unknown-node). `npm run lint` (clean) and `npm run build` (compiles + type-checks) in `apps/web`. Re-ran `packages/gpu-worker` — 14/14, no regressions. Full `./init.sh` — all green. Beyond typecheck/tests, manually drove the real browser flow: started `apps/api` (uvicorn) and `apps/web` (`next dev`), used Playwright (installed temporarily, removed afterward) to open `/tree`, click the unlocked "Loss Functions" node, submit a wrong answer (confirmed "Not quite — 0/1 correct. Try again.", modal stayed open, tree unchanged), then submit the correct answer (confirmed "Passed! (1/1 correct)"), close the modal, and confirm (via screenshots) "Loss Functions" turned green and "Gradient Descent" flipped from grey/locked to blue/unlocked immediately, with "Neural Networks" correctly still locked.
- Evidence captured: recorded in `feature_list.json` feat-008 `evidence[]`.
- Commits: not yet committed this session.
- Files or artifacts updated: `apps/api/app/services/quiz.py` (new), `apps/api/app/routers/trees.py`, `apps/api/tests/test_quiz.py` (new), `apps/api/tests/test_trees_submit_quiz_endpoint.py` (new), `apps/web/app/tree/QuizModal.tsx` (new), `apps/web/app/tree/{types.ts, unlock.ts, page.tsx, progressStore.ts}`, `feature_list.json`, `progress.md`.
- Known risk or unresolved issue: found and fixed a real environment bug during manual verification — an orphaned uvicorn process from an earlier session was still bound to port 8000 serving pre-feat-008 code, so the first live submit-quiz request returned a stale 501 even though the source on disk was already correct; killed it, started a fresh instance, confirmed via curl before re-testing in the browser (worth remembering: always confirm the running server's PID/start time matches the current code before trusting a "it's broken" signal during manual verification). No persisted server-side storage for completed status — grading is stateless per-request, and `apps/web`'s client-side `progressStore` remains the sole source of truth for progress, same as feat-006. This, plus feat-007's lack of a chunk-text store, both point at the same open gap: no storage layer has been built yet, despite `docs/hackathon-scope.md` §5 naming SQLite/JSON as the intended approach.
- Next best step: implement feat-009 (pre-indexed, deterministic demo dataset) — pick the real demo document set, run it through feat-001→feat-007 once, and save the result as the new sample tree JSON; this is also the forcing function for deciding where chunk text and completed-status ultimately get persisted.

### Session 010

- Date: 2026-07-05
- Goal: Implement feat-009, "Pre-indexed, deterministic demo dataset."
- Completed: Confirmed this environment has no reachable local Gemma/Ollama server and no `FIREWORKS_API_KEY` — the same infra gap already flagged against feat-002/003/007. Rather than faking LLM output and mislabeling it as "real pipeline output" (which is exactly what feat-009 exists to replace), built the real, reusable infrastructure and documented the credential blocker honestly instead of claiming a false pass. Added `scripts/generate_sample_docs.py`, run once to author and check in a real (not filler) 5-document intro-ML source set at `data/source_docs/` (linear algebra, calculus, probability, an ML lecture-slides deck, and a deep-learning intro — matching the same topics as docs/concept-graph-pipeline.md's own sample tree). Added `scripts/build_demo_dataset.py`, whose `build_dataset()` composes the already-unit-tested feat-001 (`chunk_document`) through feat-007 (`generate_lesson_package`) functions end to end and assembles the final nodes[]/edges[] schema; its `main()` CLI requires real `GEMMA_BASE_URL`/`FIREWORKS_API_KEY` and exits 1 with a clear message if either is missing, rather than silently substituting fakes.
- Verification run: ran feat-001's real `chunk_document()` (no fakes) against the real checked-in documents — 16 chunks across 5 files, boundary-aligned. `python -m pytest scripts/tests/` — 3/3 passed: confirms the source-docs directory is real; feeds those real chunks through `build_dataset()` with fakes injected only for Gemma/embedding/Fireworks (same external-service-fakes pattern as every other feature), asserting the DAG tier invariant and that every node gets a lesson/quiz/sources tracing back only to real ingested doc_ids. Manually confirmed the CLI's two credential guards actually fire (ran it with neither, then with only `GEMMA_BASE_URL` set — each exits 1 with the expected message). `python -m compileall scripts` (pass). Re-ran the full existing suite for regressions: apps/api 23/23, packages/gpu-worker 14/14, apps/web lint/build clean — all unaffected.
- Evidence captured: recorded in `feature_list.json` feat-009 `evidence[]`; status set to `blocked` (not `passing` and not `not_started` — genuine infrastructure work happened, but the actual credentialed run hasn't).
- Commits: not yet committed this session.
- Files or artifacts updated: `scripts/generate_sample_docs.py` (new), `scripts/build_demo_dataset.py` (new), `scripts/tests/test_build_demo_dataset.py` (new), `data/source_docs/*` (new, 5 files), `.env.example` (added missing `GEMMA_BASE_URL`), `.gitignore` (added `data/generated/` — the script's default output path, not checked in until a specific run is deliberately promoted), `feature_list.json`, `progress.md`.
- Known risk or unresolved issue: `data/atlas_mastery_tree_sample.json` (the original hand-written sample) is deliberately untouched — `apps/api/tests/test_trees_endpoint.py`/`test_trees_submit_quiz_endpoint.py` hardcode its specific node/question IDs, so swapping in real pipeline output must happen in the same pass as updating those tests, not before a real run exists to swap in. `scripts/tests/`' fakes prove the orchestration code is correct, not that the real Gemma/Fireworks calls will produce a sensible tree — that judgment can only be checked once real credentials exist. `python -m pytest scripts/tests/` is not yet wired into `init.sh`/`CLAUDE.md`'s fixed verification list, since `scripts/` isn't one of the three existing service areas — worth a deliberate decision on whether to add a fourth verification line if this directory grows.
- Next best step: this closes out every feature in `feature_list.json` except feat-009's real-infra run. Once `GEMMA_BASE_URL` and `FIREWORKS_API_KEY` are available (see docs/hackathon-scope.md §5's Day-1 ROCm check), run `python scripts/build_demo_dataset.py`, inspect the output for plausibility against docs/concept-graph-pipeline.md's sample tree shape, and decide whether/how to promote it to replace the checked-in sample file (updating the two hardcoded-ID test files in the same pass).

### Session 011

- Date: 2026-07-05
- Goal: Restyle `apps/web` to match the "Atlas Learning System" design mockups the user provided in `stitch_atlas_learning_path_graph/` (Google Stitch exports). That directory actually contained two competing visual directions for the same screens (a clean glassmorphic "Atlas Learning System" set and a gamified "Lumina Journal" quest-themed set); asked the user which to build, and how much of the app to restyle — they chose "Atlas Learning System" and "everything" (design tokens + landing page + tree page + quiz/lesson modal).
- Completed: `apps/web/tailwind.config.ts` now carries the full design-token set from `atlas_learning_system/DESIGN.md` (colors, `display-lg`/`headline-lg`/`body-md`/`stats-mono`/`label-caps` font sizes, spacing, border radii — using DESIGN.md's own radius scale rather than the inline config baked into the per-screen `code.html` exports, since that version omits a real `rounded-full: 9999px` and would turn avatars/pills into rounded squares). `app/layout.tsx` self-hosts Inter + JetBrains Mono via `next/font/google` (confirmed Google Fonts is reachable from this environment first). `app/globals.css` adds the `glass-panel`/`glow-active`/`bg-graph-pattern` utility classes and a custom scrollbar, matching the mockups' inline `<style>` blocks. Rewrote `app/page.tsx` to match `atlas_landing_upload` (hero headline, dropzone card, footer) — since there's no upload endpoint wired from the web app to `packages/gpu-worker`'s `/ingest` yet, made the dropzone a `Link` to `/tree` with an honest caption ("Opens the pre-indexed demo mastery tree") rather than a dead button or a fabricated upload call. Restyled `app/tree/TreeNode.tsx` (completed/unlocked/locked node states with lucide icons matching `.node-completed`/`.node-active`/`.node-locked`) and `app/tree/page.tsx` (added the top nav bar, left side nav with a real "% Complete" stat and non-functional-looking Documents/Settings entries since those pages don't exist, and a right sidebar with a Path Progress card and a Source Documents card deduped from all nodes' `sources[]`; styled React Flow's edges by target-node status to match the mockup's connector-line colors/glow). Redesigned `app/tree/QuizModal.tsx` into a full node detail panel matching `atlas_learning_derivatives` — header with icon+eyebrow+title+close, a Quick Lesson section and a Real-world Example section (both newly surfaced from the tree JSON's existing `lesson` field, previously fetched but unused in the UI), and the existing checkpoint-quiz flow reskinned with selected-state highlighting and a context-aware footer (lock/retry/completed states). Extended `app/tree/types.ts` with `NodeLesson`/`NodeSource` and added `lesson`/`sources` to `RawTreeNode`.
- Verification run: `npm run lint` (clean) and `npm run build` (compiles + type-checks) in `apps/web`. Manually verified live in a browser (not just typecheck/build): started `apps/api` (uvicorn) and `apps/web` (`next dev`), used Playwright (installed temporarily, then removed) to screenshot `/` and `/tree`, and visually compared side-by-side against the three `atlas_*` mockup screenshots — fonts, colors, card layout, and node styling all matched closely. Re-ran the full pass-quiz interaction end to end under the new styling: clicked the unlocked "Loss Functions" node, submitted the correct answer, confirmed "Node completed" in the footer, closed the panel, and confirmed (screenshot-verified) the node turned solid green, Path Progress moved 40%→60%, "Gradient Descent" flipped to the unlocked cyan-glow style, and "Neural Networks" correctly stayed locked/dashed — no console errors. Re-ran the backend suites for regressions: apps/api 23/23, packages/gpu-worker 14/14 — unaffected, as expected for a frontend-only change.
- Evidence captured: this is a UI/design pass over already-`passing` features (feat-006/007/008), not a new feature — no `feature_list.json` entry added or changed for it.
- Commits: not yet committed this session.
- Files or artifacts updated: `apps/web/tailwind.config.ts`, `apps/web/app/layout.tsx`, `apps/web/app/globals.css`, `apps/web/app/page.tsx`, `apps/web/app/tree/{TreeNode.tsx, page.tsx, QuizModal.tsx, types.ts}`, `progress.md`.
- Known risk or unresolved issue: the landing page's dropzone is visual-only (navigates to `/tree`, doesn't actually accept/upload files) since no web-to-gpu-worker upload flow exists yet — building that would be a new feature, not a restyle. The left side nav's "Documents"/"Settings" entries are static/muted since those pages don't exist. `next/font/google` fetches font files from Google Fonts at build time — confirmed reachable in this environment, but worth knowing if a future build environment is network-isolated (would need a self-hosted font fallback).
- Next best step: none required immediately — this was a self-contained design pass. The next substantive work is still feat-009's real-infra run (see above); if further UI work is wanted, an actual upload flow (web → gpu-worker `/ingest`) would be the natural next design/feature target.

### Session 012

- Date: 2026-07-05
- Goal: The user does not currently have real AMD ROCm/Gemma infrastructure or a Fireworks account provisioned, and asked to validate the full pipeline (steps 1-7 in docs/concept-graph-pipeline.md) end-to-end using an OpenAI API key with GPT-4o mini instead, as a prototyping stand-in, saving the real AMD/Fireworks path for later. **Security note handled first**: the user pasted a live OpenAI API key in plaintext in chat — flagged immediately that it must be treated as compromised regardless of anything done with it, and recommended rotating it at platform.openai.com. The key was only ever used as a transient shell environment variable for this session's live tests, never written to any tracked file (`.env` was not touched).
- Completed: Since Fireworks' hosted API is itself OpenAI-compatible (same `/chat/completions` schema), no new client classes were needed for prerequisite inference (feat-003) or lesson/quiz generation (feat-007) — added an `LLM_PROVIDER` env-var branch inside both `FireworksClient.__init__` implementations (`packages/gpu-worker/worker/prerequisites.py` and `apps/api/app/services/teach.py`): when `LLM_PROVIDER=openai`, resolves `OPENAI_API_KEY`/`OPENAI_BASE_URL`/`OPENAI_MODEL` (default `gpt-4o-mini`) instead of the `FIREWORKS_*` vars; explicit constructor args still always win regardless of the env var. Concept extraction (feat-002) genuinely needed new classes since Ollama's `/api/generate` shape differs from OpenAI's chat-completions shape — added `OpenAIConceptExtractor` (same `.extract(chunk)` interface as `GemmaConceptExtractor`, asks for a `{"concepts": [...]}` JSON envelope since OpenAI's JSON mode expects a top-level object) and `OpenAIEmbedder` (same `__call__(texts)` interface as `SentenceTransformerEmbedder`, calls `/embeddings` with `text-embedding-3-small`, avoiding a ~1.3GB bge-large model download for prototyping) in `packages/gpu-worker/worker/concepts.py`. Wired the same `LLM_PROVIDER` switch into `packages/gpu-worker/worker/main.py`'s module-level client construction. Updated `.env.example` with `LLM_PROVIDER`/`OPENAI_API_KEY`/`OPENAI_MODEL`/`OPENAI_EMBEDDING_MODEL`, all clearly marked prototyping-only. Also updated `scripts/build_demo_dataset.py` (feat-009's actual pipeline-runner script) to respect the same switch and added a `--prereq-similarity-threshold` CLI flag, so the project's real, shipped orchestration code — not just ad hoc scratch scripts — can be run against OpenAI.
- **Live end-to-end run** (real GPT-4o mini + text-embedding-3-small calls, not fakes): first attempt against all 5 real `data/source_docs/` files (16 chunks) ballooned to 2.3GB memory and had to be killed — traced to `build_candidate_edges`'s default `similarity_threshold=0.35` (tuned for bge-large-en-v1.5) letting through 407 of 2346 possible concept pairs against OpenAI's `text-embedding-3-small` embeddings (whose off-diagonal cosine similarities ranged -0.01 to 0.96, mean 0.25 — a very different distribution), meaning 407 sequential LLM calls, not a hang. Re-ran against 2 documents (6 chunks) with an explicit `0.6` threshold: real concept extraction (24 raw concepts, e.g. "Vector", "Gradient", "Chain Rule"), real embedding + clustering (24 canonical, no bogus merges), real prerequisite inference (8 sensible candidate edges, e.g. "Dot Product → Matrix Multiplication"), real cycle validation (0 dropped, none needed), real tiering (3 levels, correct DAG invariant), and real per-node lesson/quiz/example generation with the self-check loop — **24/24 nodes passed the alignment self-check on the first attempt**, each grounded in its own real source chunk text. This is the first time this project's full pipeline has been proven to work end-to-end against a live LLM, for any provider.
- Verification run: added real unit tests (not just the one-off live script) for everything new: `packages/gpu-worker/tests/test_concepts.py` gained `test_openai_concept_extractor_posts_to_chat_completions_and_parses_concepts` and `test_openai_embedder_posts_to_embeddings_endpoint_and_preserves_order` (fake `httpx.post`, no live key needed); `packages/gpu-worker/tests/test_prerequisites.py` and `apps/api/tests/test_teach.py` each gained 2-3 tests for the `LLM_PROVIDER` branch (default-Fireworks, switched-to-OpenAI, explicit-args-always-win). Full suite: apps/api 25/25, packages/gpu-worker 19/19, `scripts/tests/` 3/3, `apps/web` lint/build clean — all passing, no regressions from before this session. Manually confirmed both of `scripts/build_demo_dataset.py`'s credential guards still fire correctly (no env vars → GEMMA_BASE_URL error; `LLM_PROVIDER=openai` with no key → OPENAI_API_KEY error).
- Evidence captured: recorded in `feature_list.json`'s feat-002/003/007 evidence arrays (the live-run proof and the two calibration findings) and feat-009's evidence/notes (clarifying this is a complementary, non-blocking de-risking path, not a way to mark feat-009 `passing`).
- Commits: not yet committed this session.
- Files or artifacts updated: `packages/gpu-worker/worker/concepts.py`, `packages/gpu-worker/worker/main.py`, `packages/gpu-worker/worker/prerequisites.py`, `apps/api/app/services/teach.py`, `scripts/build_demo_dataset.py`, `.env.example`, `packages/gpu-worker/tests/test_concepts.py`, `packages/gpu-worker/tests/test_prerequisites.py`, `apps/api/tests/test_teach.py`, `feature_list.json`, `progress.md`.
- Known risk or unresolved issue: **the leaked API key must be rotated by the user** — this is not resolved by anything done in this session, only mitigated (never written to disk). Two real, concrete calibration findings need follow-up before any real pre-indexing run (Gemma or OpenAI): (1) `cluster_concepts`'s dedup `similarity_threshold` (0.9 default) missed at least one genuine duplicate ("Derivative" extracted from two different chunks) when using `text-embedding-3-small`, since same-name-different-wording pairs didn't clear 0.9; (2) `build_candidate_edges`'s pre-filter `similarity_threshold` (0.35 default) is calibrated for bge-large-en-v1.5 and is far too permissive for `text-embedding-3-small` (17% of all pairs vs. an intended handful) — recalibrate per embedding backend rather than assuming one threshold transfers. Neither default was changed in the shipped code (would need real evaluation against actual Gemma+bge-large behavior first, which this environment still can't do) — both are flagged as findings, not silently patched.
- Next best step: rotate the leaked key (user action). For the project: feat-009's real Gemma+Fireworks run is still the only remaining blocked item — when infrastructure is available, apply the two calibration findings above before trusting the first real run's output at face value.
