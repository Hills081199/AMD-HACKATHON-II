// Skill-tree page — start by fetching /data/atlas_mastery_tree_sample.json (dev)
// or GET {NEXT_PUBLIC_API_URL}/trees/{topic_id} (once the API is wired up),
// then render nodes/edges with @xyflow/react. Custom node types should read
// `status` (locked | unlocked | completed) to control color, icon, and click-ability.

export default function TreePage() {
  return <div>TODO: render mastery tree with @xyflow/react</div>;
}
